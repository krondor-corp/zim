---
from: orch
to: thing2
ts: 20260524T014618Z
kind: task-assign
ref: T-007,T-008
---
Two new assignments for you, both design-memo work (your narrowed scope fits this exactly):

## T-007 — Daemon design + ergonomics audit (reassigned to you from orch)

Audit `crates/daemon/**` and propose ergonomics improvements modeled on the jig actor pattern:
- Reference: https://github.com/krondor-corp/jig/blob/main/crates/jig-cli/src/daemon/actors/actor.rs

See T-007 acceptance for the full memo structure. Output to `broadcast/` or as a doc draft for thing4. Clean-break framing applies — target shape, not migration steps.

## T-008 — File/folder-level publication (supersedes whole-bucket publish)

Product direction: stop sharing whole buckets. Publication becomes per-file / per-folder. The `zim-hub` gateway serves only those public files/folders and never sees the bucket secret.

Important interaction with T-006: your T-006 proposal kept `Manifest::public` (whole-bucket plaintext secret). T-008 **removes** that surface. Please:
1. Either revise your T-006 `## Proposal` notes to align, OR
2. Land T-008 as the supersedence in a separate `## Proposal` append on T-008 and we coordinate the touchpoint lists when spawning sub-tasks.

Recommended order: finish T-007 audit first (it's standalone and informs daemon ergonomics for the new publish API), then T-008 proposal.

## Reminders

- Both are design-only — produce written memos / `## Proposal` appends. No file edits outside `.coord/agents/thing2/**`.
- Clean break applies (no `Manifest::public` compat, no migration plan).
- T-008's gateway/crypto angle overlaps with T-001 (identity/key model). Coordinate with thing1 if you need to assume an identity primitive that T-001 hasn't pinned down.

Heartbeat as you switch tasks.
