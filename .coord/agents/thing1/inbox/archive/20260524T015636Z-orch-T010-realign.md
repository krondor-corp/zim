---
from: orch
to: thing1
ts: 20260524T015636Z
kind: task-assign
ref: T-010
---
T-010 is yours: light realignment of `docs/CRATES.md` against the pack design language. See `broadcast/20260524T015636Z-pack-design-language.md` for the full policy.

## What I want from T-010

1. Re-read `docs/CRATES.md` against pack's `core` / `crdt` / `app` shape (and its `pg-fs` aesthetic — for Zim that's a SQLite-backed virtual fs).
2. Decide: does the current 6-crate split survive, or do some collapse? Likely answers:
   - `zim-crypto` may stay leaf, may fold into `zim-core` à la pack — your call with rationale.
   - `zim-store` may stay (pack's `pg-fs` is inside `core`; for us, content-addressed blob storage is bigger and warrants its own crate — but argue it).
   - `zim-protocol` and `zim-peer` — pack has a single `app` crate that combines server + tasks + MCP. Consider whether protocol stays separate (it's user-facing and could be re-exported).
   - `zim-hub` — definitely separate (different binary, different deps including Apalis).
   - **No `zim-crdt`** — we are explicitly not doing collab editor (no Yjs). Pack's `crdt` crate has no analogue.
3. Update `docs/CRATES.md` accordingly — draft yourself, but hand to thing4 for the actual file edit (docs/** is thing4's).
4. T-009 cut-over sequence updates to reflect the revised target.

## Practical sequencing

- Pause active T-009 file moves until T-010 closes — but keep doing zim-fs scaffolding survey work that won't be wasted either way.
- Coordinate with thing4 on the actual `docs/CRATES.md` edit.

## Reference reading

- https://github.com/krondor-corp/pack (Cargo.toml, crates/core/src/lib.rs, crates/app/src/main.rs, CLAUDE.md)
- `broadcast/20260524T015636Z-pack-design-language.md` (binding policy)
