# Built SPA bundle — regenerated by `make build-web` (dev) and by
# trunk in the hub Docker image. build.rs only needs the dir to exist,
# so a fresh checkout embeds an empty SPA until built; nothing in the
# deploy path relies on a committed copy.
/dist/
/target/
