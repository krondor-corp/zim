---
id: T-017b
title: zim-wasm JWT signing + device-approval signature
created_by: thing5
created_at: 20260524T183254Z
assignee: thing5
files_expected:
  - crates/zim-wasm/src/lib.rs
  - crates/zim-wasm/Cargo.toml
  - crates/zim-hub/static/vendor/zim-wasm/**
priority: high
parent: T-017
---
## Goal
Extend zim-wasm with two new `#[wasm_bindgen]` exports per T-017 Decision 6 + Decision 4.

## Acceptance
- [ ] `signJwt(claims_json: string) -> string` — constructs an EdDSA-signed compact JWS using the in-session ed25519 secret key. Header: `{"alg":"EdDSA","kid":"<device_id>"}`. Returns the compact serialization `header.payload.signature`.
- [ ] `signApproval(pending_id: string, new_pubkey: Uint8Array, expiry_unix: number) -> Uint8Array` — produces an ed25519 signature over `(pending_id || new_pubkey || expiry_unix)` for the device-bootstrap push-approval flow (T-017 Decision 4 step 9).
- [ ] Existing exports unchanged.
- [ ] Bundle re-vendored at `crates/zim-hub/static/vendor/zim-wasm/`.
- [ ] README updated with new function signatures.
- [ ] `cargo build/clippy/fmt -p zim-wasm` green.

## Out of scope
- Hub-side JWT verification (T-017a, thing3).
- CLI JWT signing (T-017c, thing1).

## Notes
- 20260524T183254Z thing5: Self-claimed per T-017 Decision 8 + orch ack.

## Closing (20260524T184752Z thing5)

All acceptance items met:
- [x] `signJwt(claims_json) -> string`: EdDSA compact JWS. Parses `device_id` from claims for `kid` header. base64url encodes header+payload, signs with session key, returns compact serialization.
- [x] `signApproval(pending_id, new_pubkey, expiry_unix) -> Uint8Array`: concatenates `pending_id || new_pubkey || expiry_be32`, signs with session key, returns 64-byte signature.
- [x] Existing exports unchanged.
- [x] Bundle re-vendored + `.gitignore` post-step applied.
- [x] `base64 = "0.22"` added to Cargo.toml for base64url encoding.
- [x] `cargo build/clippy/fmt -p zim-wasm` green.
- [x] Bundle delta: `.wasm` 289 KB → 313 KB (+24 KB base64+signing), `.js` 17 KB → 19 KB (+2 KB glue).

T-017b done.
