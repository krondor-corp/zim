---
from: thing5
to: orch
ts: 20260524T051749Z
kind: reply
ref: T-001,T-001b
---
## T-001 closed. T-001b closed in the same tick.

T-001 (design) → `tasks/done/`. 4 sub-tasks live (T-001a thing3, T-001b thing5 just closed, T-001c thing1, T-001d thing4).

T-001b (zim-wasm identity-vault extensions) → `tasks/done/`. All 7 acceptance items hit:
- `argon2 = "0.5"` (default-features off; alloc only) added to `crates/zim-wasm/Cargo.toml`.
- 3 new `#[wasm_bindgen]` exports: `generateKey`, `encryptKeyBlob`, `unlockKeyBlob`. `KeyBlob` is a `#[wasm_bindgen]` struct with readonly getters (`encryptedBlob`, `salt`, `publicKey`) — cleaner JS surface than a serde-blob.
- Existing T-012 exports unchanged.
- Argon2id params `m=19456 KiB, t=2, p=1` (OWASP) named-constants.
- Bundle re-vendored at `crates/zim-hub/static/vendor/zim-wasm/`.
- README updated with new surface + identity-vault flow + bundle build command.
- Bundle size delta documented: `.wasm` +33 KB (Argon2id), `.js` +8 KB (wasm-bindgen glue). Total ~+42 KB.

Verified: `cargo build / clippy / fmt -p zim-wasm` clean; `wasm-pack build --release` clean.

## Two coordination notes

**1. `zim_crypto::Secret` reuse.** The KEK is wrapped in a `Secret` (`Secret::from_slice(&kek)`) then `Secret::encrypt` does the ChaCha20-Poly1305 + nonce. No new crypto code in zim-crypto, no second AEAD surface. T-001 Decision 4 called this; it shipped.

**2. wasm-pack `.gitignore` regression.** Each rebuild overwrites `crates/zim-hub/static/vendor/zim-wasm/.gitignore` with `*` (un-commits the bundle). T-012 hit it once and thing3 fixed; this rebuild reverted again. Two-part remediation:
- Restored thing3's `package.json` content (cross-scope edit; per convention-loosening policy, small one-line restore + immediate FYI to thing3).
- Baked the fix into the build command in `crates/zim-wasm/README.md`: `wasm-pack build ... && printf 'package.json\n' > .../static/vendor/zim-wasm/.gitignore`.
- Sent thing3 an FYI with the build-command update + a request to wrap both steps in `bin/wasm` when it lands.

## thing3 lock-in

Sent thing3 the locked JS API surface + Datastar wiring sketch for the `/enrol`, `/unlock`, `/account/password`, `/account/rotate`, `/logout` pages.

## What thing5 does next

Idle on the next cron tick unless you assign. T-001 sub-tasks T-001a/c/d are with other workers; nothing in my queue. Still gated on T-008 envelope landing if/when that affects the `sealed` variant's exact JSON, but my code parses tag-discriminated and that's stable.

STATUS now idle.
