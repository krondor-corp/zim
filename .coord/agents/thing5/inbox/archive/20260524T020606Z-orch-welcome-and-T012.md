---
from: orch
to: thing5
ts: 20260524T020606Z
kind: task-assign
ref: T-012
---
Welcome. You were spun up by user to own the `zim-wasm` crate — the browser-side WASM artifact sourced by `zim-hub`'s served pages.

## First steps

1. Read `.coord/PROTOCOL.md` for the coordination rules.
2. Read `.coord/agents/thing5/ROLE.md` (orch set this up — verify it matches your understanding; edit if needed).
3. Read both binding broadcasts:
   - `.coord/broadcast/20260524T014147Z-clean-break-policy.md` — clean-break tenet (no deprecation, no migration framing).
   - `.coord/broadcast/20260524T015636Z-pack-design-language.md` and `.coord/broadcast/20260524T015900Z-pack-is-aesthetic-only.md` — pack as aesthetic reference; Datastar over HTMX; SQLite stays; no Yjs collab.
4. Read other workers' `ROLE.md` to know the neighbours:
   - **thing1** (crate cut-over, owns most non-hub crates incl. zim-crypto which you'll likely depend on)
   - **thing2** (design-only worker: protocol, daemon ergonomics, file/folder publish)
   - **thing3** (desktop removal + Datastar UI plan — their parity checklist reserves WASM as v2; that's you)
   - **thing4** (git + docs + wiki)
5. Heartbeat with first impressions.

## Your first task: T-012

Path: `.coord/tasks/claimed/T-012.md` (already assigned to you).

T-012 is **design + scaffold**, not implementation. The acceptance criteria walk you through the decisions first (role, build pipeline, JS interface, crate skeleton, workspace registration). Implementation tasks get spawned from the decisions you make.

Key decision: what does the WASM client actually *do*? Options laid out in T-012 acceptance. zim-hub never sees bucket secrets (per T-008), so client-side decryption of published content is a strong candidate; the future Milkdown-style **non-collaborative** editor surface is another.

## Coordination flags

- `crates/zim-wasm/` does not exist yet — you'll create it.
- Root `Cargo.toml` is **thing1's**. Draft the workspace registration edit and pass it to thing1 (send a message; don't edit directly).
- `crates/zim-hub/` is on T-002 (still unassigned). You define your half of the JS interface; whoever takes T-002 wires the script tag + bundle path.
- **No commits.** thing4 owns git ops; hold until orch/user signals.

## Posture

- Design-only until the scaffold decisions are documented (in T-012 `## Notes` as a `## Proposal` append, like thing2 did on T-006).
- Heartbeat on every state change; at minimum every 15 minutes while active.
- Ask early; flag blockers immediately rather than guessing.
