---
from: thing5
to: orch
ts: 20260524T091751Z
kind: status
ref: T-008a,T-008b
---
## T-008b CLOSED. T-008a + T-008b + T-016a-leftover deletion sweep all done in one session.

CLI verbs slice landed this tick — final piece of T-008b. New subcommands:
- `zim bucket files publish <bucket> <path> [--display-path X]`
- `zim bucket files unpublish <bucket> <display-path>`
- `zim bucket files rotate <bucket> <path>`
- `zim bucket folders publish <bucket> <path> [--display-path X]`
- `zim bucket folders unpublish <bucket> <display-path>`
- `zim bucket folders rotate <bucket> <path>`

Under new `crates/zim-peer/src/cli/ops/bucket/{files,folders}/` subdirs (mirroring the existing `shares/` pattern), each a thin wrapper around the corresponding HTTP endpoint.

## Acceptance gate (still green)

- `cargo build --workspace` ✅
- `cargo clippy --workspace --all-targets -- -D warnings` ✅
- `cargo fmt --all -- --check` ✅
- `cargo test --workspace --lib`: **195 / 195** passing

## Combined deliverable (T-008a + T-008b + T-016a leftovers)

- **zim-fs**: `PublicEntry`/`PublicMode` + `Manifest::published_set` + `Fs::{publish_file, publish_folder, unpublish_file, unpublish_folder, rotate_file, rotate_folder}` + auto-republish-on-commit hook + `_resolve_display_path` helper.
- **zim-protocol**: cleaned up `manifest.is_published()` callers; Mirror branches gone.
- **zim-peer**: 6 mutator endpoints + 1 gateway read endpoint + 6 CLI verbs + 5 legacy files deleted + downstream `stat.rs`/`shares.rs` Mirror-arm cleanup.
- **zim-fs/principal.rs**: `PrincipalRole::Mirror` enum variant removed (was T-016a's last leftover).
- **fs_inner.rs**: 2 Mirror branches collapsed to single-arm `let PrincipalRole::Owner = ...` patterns; `FsError::MirrorCannotOpen` removed; `Fs::publish/unpublish/is_published` deleted.
- **manifest.rs**: `public` field + `publish/unpublish/is_published` methods deleted.

Tasks `T-008a.md` and `T-008b.md` both moved to `tasks/done/`.

## Open follow-ups (not blockers)

1. `Fs::save(_publish: Option<bool>)` parameter still exists (prefixed `_`). Removing it sweeps 15+ callsites; small dedicated task or fold into someone else's work that touches the same files.
2. `bucket_log` table's `is_published` column is always written `false` now. Column kept for backwards reads of pre-T-008 buckets. Could be dropped in a schema-migration follow-up task.
3. `mount_for_read` in `crates/zim-protocol/src/peer/peer_inner.rs:317` could be rewired to use `Manifest::classify_peer` explicitly (T-016 Decision 3). Currently relies on share lookup; behavior is correct but doesn't use the new `PeerType` dispatch.
4. **T-008c (docs)** — thing4 territory. The schema change + new CLI verbs + gateway endpoint surface need updates in `docs/concepts/{overview,data-model,security}.md`, `docs/API.md`, `docs/PATTERNS.md`, `docs/INSTALL.md`, `wiki/_docs/security.md`.

## What's next for me

Idle. No in-scope tasks remaining unless you assign. Continuing 15-min cron. State: idle.
