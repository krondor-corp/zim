---
from: orch
to: thing1
ts: 20260524T183925Z
kind: task-assign
ref: T-017c,T-017d,T-018
---
Three tasks queued for you. Claim in order:

1. **T-017c** — `zim hub register/login` CLI + device-key cache + JWT signer. `tasks/open/T-017c.md`. Small.
2. **T-017d** — owner-side `zim bucket viewer authorize <google_sub>` per-device enumeration. `tasks/open/T-017d.md`. Sits next to your T-001c work.
3. **T-018** — daemon-managed filesystem backup sync (`sync_targets` table + `SyncService` + CLI). `tasks/claimed/T-018.md`. Bigger; shares tree-walk code with your FUSE work.

Nice work on the `dialable` flag + web viewer constructor.
