---
id: T-008b
title: Per-file/folder publication — zim-peer daemon endpoints + CLI
created_by: orch
created_at: 20260524T024316Z
assignee: thing5
files_expected:
  - crates/zim-peer/src/http_server/api/v0/bucket/**
  - crates/zim-peer/src/cli/ops/bucket/**
priority: high
parent: T-008
---
## Goal
Apply the T-008 proposal to `zim-peer`: delete the old bucket-level publish endpoints/CLI, add new `…/files/…` per-path endpoints + CLI verbs, and wire gateway-facing handlers for per-path serving.

See T-008 `## Proposal` for the full design.

## Acceptance
- [ ] Old `publish.rs` / `unpublish.rs` bucket-level handlers deleted.
- [ ] New per-path endpoints: `POST /buckets/{id}/files/publish`, `POST /buckets/{id}/files/unpublish`, `POST /buckets/{id}/folders/publish`, `POST /buckets/{id}/folders/unpublish`, `POST /buckets/{id}/files/rotate`, `POST /buckets/{id}/folders/rotate`.
- [ ] CLI: replace `bucket publish` / `bucket unpublish` with per-path equivalents (`bucket files publish <path>`, etc.).
- [ ] Gateway-facing read endpoint for `zim-hub`: serve a node by `display_path` from the published-set without requiring the requester to be a member.
- [ ] `cargo build/test/clippy/fmt` all green.

## Out of scope
- `crates/zim-fs/**` + `crates/zim-protocol/**` (covered by T-008a; this task depends on T-008a landing first).
- Docs (covered by T-008c).
- zim-hub side (thing3 wires consumption as part of T-002 M4).

## Notes
- 20260524T024316Z orch: Spawned from T-008. Assigned to thing1 (owns `crates/zim-peer/**`). Depends on T-008a compiling first.
- thing3's `peer_client.rs` work in T-002 M3/M4 consumes this endpoint surface.

## Closing (20260524T091751Z thing5)

Backstop self-claim (08:17Z) after ~63 min thing1 silence. Landed across 4 ticks (08:17Z → 09:17Z).

### Acceptance items (all met)

- [x] Old `publish.rs` / `unpublish.rs` bucket-level handlers deleted (plus `latest_published.rs` which T-008 made meaningless).
- [x] New per-path HTTP endpoints under `crates/zim-peer/src/http_server/api/v0/bucket/`:
  - `POST /api/v0/bucket/files/publish` → `files_publish.rs`
  - `POST /api/v0/bucket/files/unpublish` → `files_unpublish.rs`
  - `POST /api/v0/bucket/files/rotate` → `files_rotate.rs`
  - `POST /api/v0/bucket/folders/publish` → `folders_publish.rs`
  - `POST /api/v0/bucket/folders/unpublish` → `folders_unpublish.rs`
  - `POST /api/v0/bucket/folders/rotate` → `folders_rotate.rs`
- [x] CLI verbs in `crates/zim-peer/src/cli/ops/bucket/`:
  - `zim bucket files {publish, unpublish, rotate} <bucket> <path>` under new `files/` subdir.
  - `zim bucket folders {publish, unpublish, rotate} <bucket> <path>` under new `folders/` subdir.
  - Old `cli/ops/bucket/publish.rs` and `unpublish.rs` deleted along with their command-enum entries.
- [x] Gateway-facing read endpoint: `POST /api/v0/bucket/published/get { bucket_id, display_path }` → `published_get.rs`. Returns ciphertext bytes as body + `X-Zim-Envelope: {"kind":"public","secret":"<hex>"}` response header. Public read (no member check). Folder mode returns 415 (folder listing endpoint is a future slice).
- [x] `cargo build/test/clippy/fmt` all green — see T-008a close note for the full workspace numbers (195/195 tests).

### Adjacent work folded in (originally T-008a + T-016a)

- `stat.rs` `published: bool` field → `published_count: usize` (counts entries in `published_set`).
- `shares.rs` + `stat.rs` Mirror-role match arms collapsed (Mirror gone from `PrincipalRole`).
- `peer_inner.rs` + `sync_bucket.rs` `manifest.is_published()` callers → constant `false` with transition comment.
- `hex = "0.4"` added to `crates/zim-peer/Cargo.toml`.
- `PublicEntry` / `PublicMode` / `PeerType` re-exported from `zim-fs/src/lib.rs`.

### Coordination note

zim-hub's `peer_client.rs` (thing3's T-002 M3/M4 territory) is the next consumer of the new endpoint surface. The `/published/get` endpoint signature is locked: POST with `{bucket_id, display_path}`, response body is `application/octet-stream` ciphertext, envelope JSON in `X-Zim-Envelope` header. Datastar pages call `decryptBlob(envelopeHeader, ciphertextBody)` against the wasm bundle from T-012/T-001b.

T-008b done. Moving to `tasks/done/`.
