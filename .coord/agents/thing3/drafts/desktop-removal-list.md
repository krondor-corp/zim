# Desktop removal list

**Author:** thing3 (T-003)
**Policy:** clean-break (`broadcast/20260524T014147Z-clean-break-policy.md`)
**Authoritative cut-over:** `docs/CRATES.md` (T-005) already lists `crates/desktop/` and `crates/app/` as deleted. This doc enumerates the surrounding code/docs/CI/scripts that also have to go.
**Application owner:** thing4 (for docs/** and root-level files); thing1 / cut-over owner for workspace + filesystem deletions.

## A. Filesystem deletions (whole-tree)

| Path | Notes |
|---|---|
| `crates/desktop/` | Entire crate. Includes `src/` (SolidJS), `src-tauri/` (Tauri), `dist/`, `node_modules/`, `package.json`, `pnpm-lock.yaml`, `vite.config.ts`, `tsconfig.json`, `index.html`, `.gitignore`. |
| `crates/app/` | Empty/cruft skeleton (`here/blobs/...`, `src/daemon/...`). Listed as deleted in `docs/CRATES.md` already. |

## B. Cargo workspace edits

`Cargo.toml` (workspace root):

```diff
-members = ["crates/daemon", "crates/common", "crates/object-store", "crates/desktop/src-tauri"]
+members = ["crates/daemon", "crates/common", "crates/object-store"]
```

(Final shape of `members` is owned by the T-005 cut-over and becomes the six `zim-*` crates; this diff is the desktop-only delta.)

`Cargo.lock`: regenerated by next build — no manual edit.

## C. GitHub Actions workflows

| File | Action | Notes |
|---|---|---|
| `.github/workflows/release-desktop.yml` | **delete entirely** | Tauri-only release pipeline. |
| `.github/workflows/ci-tauri.yml` | **delete entirely** | Tauri-only CI. |
| `.github/workflows/ci.yml` | **edit** | Remove `desktop-ci` job and its `needs:` reference from `ci-success`. See diff below. |
| `.github/workflows/release-pr.yml` | **edit** | Remove every `jax-desktop` block: `LATEST_DESKTOP_TAG`, `DESKTOP_CHANGES`, `DESKTOP_VERSION`, the comment on `publish=false`, and the manual sed-bump of `crates/desktop/{src-tauri/Cargo.toml,src-tauri/tauri.conf.json,package.json}`. |
| `.github/workflows/publish-crate.yml` | **edit** | Drop `DESKTOP_VERSION`, `jax-desktop v$DESKTOP_VERSION` echo, and `"jax-desktop-v$DESKTOP_VERSION"` from the tag loop. |
| `.github/workflows/release-cli.yml` | **review** | If it references desktop, drop. Likely safe — name suggests CLI-only. |

### `ci.yml` diff

```diff
-  # Desktop app CI (Tauri + SolidJS)
-  desktop-ci:
-    name: Desktop CI
-    uses: ./.github/workflows/ci-tauri.yml
-
   ci-success:
     name: CI Success
     runs-on: ubuntu-latest
-    needs: [rust-ci, desktop-ci]
+    needs: [rust-ci]
```

## D. Dev scripts (`bin/`)

| File | Edit |
|---|---|
| `bin/test` | Drop `--exclude jax-desktop` (crate gone). |
| `bin/check` | Drop both occurrences of `--exclude jax-desktop`. |
| `bin/build` | Drop `--exclude jax-desktop`. |

```diff
-cargo test --workspace --exclude jax-desktop ${CARGO_FEATURES:---all-features}
+cargo test --workspace ${CARGO_FEATURES:---all-features}
```

(Same shape for `cargo clippy`, `cargo check`, `cargo build`.)

## E. Docs deletions and rewrites

| File | Action | Detail |
|---|---|---|
| `docs/INSTALL.md` | **delete sections** | Lines 71–166: the entire "Desktop App (Recommended for most users)" section, "Building Desktop App from Source" subsection, "Gentoo desktop build dependencies" note. Replace with a stub: "Single binary: `zim-peer` (CLI/daemon). Optional read-only web hub: `zim-hub` — see `docs/concepts/zim-hub.md`." Final shape lands once T-002 produces the hub doc. |
| `docs/PATTERNS.md` | **delete section** | Lines 681–713: "Tauri Desktop Patterns" subsection (`include_bytes!` note + "Tauri Setup Errors"). Remove without replacement — Tauri is gone. |
| `docs/PROJECT_LAYOUT.md` | **edit** | Line 9: drop "(used by Tauri)" from the jax-daemon library description. Lines 101–114: delete the entire `crates/desktop` section. Line 144: drop `release-desktop.yml` from the releases list. PROJECT_LAYOUT.md will be rewritten wholesale by T-005's cut-over; this is the desktop delta only. |
| `docs/concepts/index.md` | **edit** | Line 43: rewrite to drop the "Tauri desktop app to embed the daemon in-process" example. Replace with a generic phrasing: "embedding into host applications (e.g. the future zim-hub web mirror)." |
| `docs/concepts/fuse-architecture.md` | **edit** | Line 149: change "calls the same endpoints that the CLI and desktop app use" → "calls the same endpoints the CLI uses". |
| `docs/CRATES.md` | **leave** | Already lists `crates/desktop/` as deleted (line 32, line 82, line 92). No edit. |
| `README.md` | **delete sections** | Lines 18–24 ("Desktop App" with three download links), lines 42–49 (download table), line 75 (`jax-desktop` crate entry). Replace top section with a one-liner pointing to the headless daemon + future zim-hub. |
| `CLAUDE.md` | **edit** | Line 31: drop the `desktop/` entry from the crate tree comment. Aligns with T-005 target shape. |

### Docs the orchestrator/thing4 should NOT touch in this delta

- `docs/CRATES.md` — owned by T-005, already current.
- `docs/CONTRIBUTING.md`, `docs/DEVELOPMENT.md`, `docs/DEBUG.md`, `docs/SUCCESS_CRITERIA.md`, `docs/CLI.md`, `docs/API.md`, `docs/RELEASE.md` — verified no desktop refs.

## F. Root-level housekeeping

| File | Edit |
|---|---|
| `install.sh` | If it references the desktop bundle, drop. (Current scan: no desktop refs found.) |
| `Dockerfile` / `docker-compose.yml` / `docker-entrypoint.sh` | No desktop refs — leave. |
| `Makefile` | No desktop refs — leave. |
| `release.toml` | No desktop refs — leave. |
| `build.rs` | No desktop refs — leave. |

## G. Order of operations (suggested)

1. Workspace `members` edit (B) and `bin/{test,check,build}` (D) — small, unblocks CI.
2. Filesystem deletions (A).
3. Workflow deletions/edits (C).
4. Docs/README edits (E) — thing4.
5. CLAUDE.md edit (E) — thing4.

`cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt -- --check` must remain green after each step (or, per T-005's policy, at the cut-over end state only).

## H. What is NOT in this list

- Renames from `jax-*` to `zim-*` (covered by T-005 cut-over).
- The new `crates/zim-hub/` scaffold (covered by T-002).
- `crates/common/`, `crates/daemon/`, `crates/object-store/` moves/renames (T-005).
- Mirror role removal (T-006).
- `Manifest::public` replacement (T-008).
