---
id: T-011
title: Reshape docs/ structure to pack's layout
created_by: orch
created_at: 20260524T015636Z
assignee: null
files_expected:
  - docs/**
priority: normal
---
## Goal
Reshape Zim's `docs/` to match pack's layout: `getting-started.md`, `concepts/`, `architecture/`, `reference/`, `deployment/`, plus the kept-flat files (`PATTERNS.md`, `CONTRIBUTING.md`, `SUCCESS_CRITERIA.md`, `CRATES.md`). Clean break — files that don't fit the target structure get moved or merged outright.

Reference: pack's `docs/` listing in CLAUDE.md.

## Acceptance
- [ ] Mapping table: current `docs/*` → target `docs/<subdir>/<file>.md` or `docs/<file>.md`.
- [ ] `docs/getting-started.md` exists (distilled from `INSTALL.md` + `index.md`).
- [ ] `docs/concepts/` retained (matches pack); audit which existing concept pages stay.
- [ ] `docs/architecture/` created with content extracted from the existing concepts/* (runtime, services, events analogues).
- [ ] `docs/reference/` created (HTTP routes, MCP tools, database models).
- [ ] `docs/deployment/` created (Kamal placeholder; `iac/` and `.kamal/` to follow under a separate task).
- [ ] Execution coordinated between thing1 (drafts) and thing4 (applies edits + does git moves).

## Out of scope
- Wiki content migration (T-004 wiki phases).
- New deployment infrastructure (`iac/`, `.kamal/` scaffolds) — separate task later.
- Concept-page rewrites for the mirror-role removal (T-006c).

## Notes
- 20260524T015636Z orch: Created in response to pack-design-language broadcast. Coordinates with thing4 (docs owner) and depends on T-006c (mirror role removal) landing first to avoid double-touching the same pages.
