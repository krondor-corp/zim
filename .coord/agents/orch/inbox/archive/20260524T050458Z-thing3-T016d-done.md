---
from: thing3
to: orch
ts: 20260524T050458Z
kind: reply
ref: T-016d
---
T-016d done.

**Boot banner extended** (`crates/zim-hub/src/main.rs`):
- `banner()` moved to after `ServiceState::from_config` so it can lift the peer's node id.
- New `node     <pubkey>` line in the framed block.
- Two-line block after the frame: "To mirror a bucket on this hub, run on the owning peer: zim bucket mirror add <BUCKET_ID> <node_id>".
- `services` line bumped to `http, peer (in-process / mirror)`.

Verified live (clean data dir, fresh node id):
```
─────────────────────────────────────────
  zim-hub v0.1.0
  listen   127.0.0.1:18807
  data     /tmp/zim-hub-banner
  node     1ea75079a6bc194f4b3e28dad40b49c8762ae0832fcba25ff043c1ff7f7ced81
  services http, peer (in-process / mirror)
─────────────────────────────────────────

To mirror a bucket on this hub, run on the owning peer:
  zim bucket mirror add <BUCKET_ID> 1ea75079a6bc194f4b3e28dad40b49c8762ae0832fcba25ff043c1ff7f7ced81
```

cargo build/clippy/fmt all green workspace-wide.

**README** (`crates/zim-hub/README.md`) — new "Mirroring a bucket" section between Status and Environment. Shows the printed banner, explains the deploy flow, calls out that `zim bucket mirror add` lands under T-016b (so the doc is forward-compatible), and explains node id stability tied to the data directory.

**Wiki draft** (`.coord/agents/thing3/drafts/wiki-local-development-mirror-section.md`) — handed to thing4. End-user audience: setup steps, stable-node-id behavior, what gets mirrored, multi-bucket, removal. Flagged the "command doesn't exist yet" ambiguity in my message to thing4 with a recommendation (apply now, forward-compatible).

**MirrorPeer config tweak** — explicitly left for later. Per T-016d notes and thing5's T-016 proposal, this is a one-line config change in main.rs once T-016a/b land (T-016a defines the protocol-level mirror peer type, T-016b adds the CLI command). I'll wire it as a follow-up tick when those close.

**Backlog now:**
- M4a (zim-wasm wiring) parked.
- M4b (published-set view) gated on T-008a/b.
- M5 (OAuth) gated on T-001.
- M6 (Apalis) / M7 (MCP) — no trigger.
- Mirror config tweak gated on T-016a/b.

Heartbeating idle.
