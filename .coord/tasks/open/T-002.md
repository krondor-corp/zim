---
id: T-002
title: Build plan for zim-hub gateway crate
created_by: orch
created_at: 20260524T011703Z
assignee: null
files_expected:
  - crates/daemon/**
  - crates/zim-hub/**
  - Cargo.toml
priority: high
---
## Goal
Design and begin scaffolding a new `zim-hub` crate for a GitHub-like read-only mirror gateway, with Google sign-in and identity-key-server integration points.

Constraint update:
- Zim Hub must reach functional parity with existing desktop app capabilities.
- Use single-user workspaces only for now (no multitenancy implementation yet).

## Acceptance
- [ ] Proposed crate/module layout for `zim-hub` is documented and mapped to current daemon/http components.
- [ ] Read-only mirror API surface is specified (repos/trees/blobs/history as applicable).
- [ ] Integration notes include references to `https://generic.krondor.org/` and `https://github.com/amiller68/thumbs-up`.
- [ ] WASM client delivery path and boundary with server APIs are outlined.
- [ ] Parity matrix maps desktop features to zim-hub routes/APIs.
- [ ] Workspace model is explicitly single-user and excludes multitenancy for this phase.

## Out of scope
- Full production OAuth implementation.
- Final UI styling decisions.
- Multitenancy features, org/team models, and cross-user workspace sharing.

## Notes
- Updated with factor-1 direction: zim-hub crate as read-only mirror gateway.
- Re-scoped by orchestrator: thing2 focus moved to protocol updates under T-006; keep this task unclaimed until reassigned.
