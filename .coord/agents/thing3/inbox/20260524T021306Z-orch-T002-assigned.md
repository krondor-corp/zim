---
from: orch
to: thing3
ts: 20260524T021306Z
kind: task-assign
ref: T-002
---
## T-002 (zim-hub crate) — you own it. Start scaffolding.

This task has been sitting open and unassigned. User flagged it. Assigning to you since you designed it.

## Why you

Your T-003 deliverables are the de facto T-002 plan already:
- `.coord/agents/thing3/drafts/datastar-adoption-plan.md` (§§ 1-11) — stack, vendored datastar.js, template patterns, SSE merge-fragment routes, **first-milestone checklist for T-002 to scaffold against**.
- `.coord/agents/thing3/drafts/zim-hub-parity-checklist.md` — route shape (`/b/{id}/tree/*`, `/b/{id}/blob/*`, `/b/{id}/raw/*`, `/at/{link_hash}/...`), parity matrix.

Scaffold against your own plan. Faster than handing off.

## ROLE expanded

`files_owned` now includes `crates/zim-hub/**`. Other constraints unchanged. Still draft any `docs/**` edits for thing4. Still coordinate with thing1 for workspace-dep additions (root `Cargo.toml`).

## First steps for T-002

Per the acceptance criteria in `tasks/claimed/T-002.md`:
1. Create `crates/zim-hub/{Cargo.toml, src/main.rs, src/lib.rs}` skeleton against your first-milestone checklist.
2. Decide the four "adopt-from-pack" questions explicitly (one-line yes/no/later + rationale each): Apalis, MCP endpoint, runtime::Service, struct-patch.
3. Draft the workspace-registration edit for root `Cargo.toml` and send it to thing1 (do not edit root `Cargo.toml` yourself; thing1 owns it).
4. Land a minimum-viable `cargo build` green for the zim-hub crate (even if `main.rs` just prints "zim-hub starting…").
5. Heartbeat with state at each milestone.

## Coordination flags

- **thing5 (zim-wasm)** is designing the WASM client artifact you'll serve. Coordinate the bundle path and script-tag wiring. thing5 is currently blocked awaiting orch ack on their Decision 6 (zim-crypto wasm feature flag) — that flow doesn't block your skeleton, but the script-tag integration will need their bundle path.
- **thing2** (T-008 file/folder publish) defines the published-set surface. Your read-only routes consume it. If you scaffold ahead of T-008's proposal, stub the published-set lookup.
- **T-001** (identity/key model) is unclaimed; your auth seam (`src/auth/google.rs`, `src/auth/session.rs`) stubs the interface — fine.

## Other current work touching this

- thing1 just finished T-009 cut-over; workspace is at 5 crates (zim-crypto/store/fs/protocol/peer). Add zim-hub as the 6th.
- thing4 holds commits — do NOT commit; just land the green build.

Heartbeat as you begin.
