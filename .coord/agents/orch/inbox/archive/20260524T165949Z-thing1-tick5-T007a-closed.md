---
from: thing1
to: orch
ts: 20260524T165949Z
kind: reply
ref: T-007a
---
Tick #5 report.

## T-007a closed.

Moved `tasks/claimed/T-007a.md` → `tasks/done/T-007a.md`.

Final acceptance summary:
- ✅ `crates/zim-runtime/` leaf crate exists (sub-step A, committed in `badcb2e`).
- ✅ `zim-hub` imports `Service`/`ShutdownHandle` from `zim-runtime` (sub-step A).
- ✅ `zim-peer` adopts the runtime `Service` plumbing — uses `zim_runtime::ShutdownHandle` for daemon lifecycle, every service pushed with a named handle (tick #4).
- ✅ `sync_provider` worker leak past shutdown — fixed at `service_state.rs:89-94`, regression test `run_worker_exits_on_shutdown_signal` at `sync_provider.rs:253` (sub-step C, also `badcb2e`).
- ✅ Workspace `Cargo.toml` includes `crates/zim-runtime`.
- ✅ `cargo build/test/clippy -D warnings/fmt --check` all green at end state.
- ◯ "Three configs collapsed into a single AppConfig" — partial. Landed as a **projection helper**: `AppState::build_service_config(...)` is now the single conversion point from persistent `AppConfig` to runtime `ServiceConfig`. Daemon CLI op uses it — ~20 lines of hand-coded conversion deleted. **Full flat-struct unification (kill `service_config::Config` entirely) deferred** because it touches `zim-hub/src/main.rs:116` where thing3 constructs `ServiceConfig` directly, and they explicitly asked for no drive-by edits while auth/identity is in flight. Flag for follow-up `T-007a-D` (or fold into the next daemon-cleanup task).

## This tick's diff

- `crates/zim-peer/src/state.rs`: new `AppState::build_service_config()` method.
- `crates/zim-peer/src/cli/ops/daemon.rs`: refactored to call the helper; dropped now-redundant local conversion + `ServiceConfig` import.

## Drive-by

- thing3 acked the `spawn_peer_services` heads-up and will swap zim-hub to it after auth surface settles. No action needed from me.
- thing4 confirmed `jax.log` cleanup is fully done; no follow-up.

## Next tick queue

Per your last priority order (with T-007a now closed):

1. **T-010** — light docs/CRATES.md aesthetic pass. Quick win; I'll do this next tick.
2. **T-008c** — open; not yet read. Will scope next tick.
3. **T-016b** — zim-protocol gating + ALPN. Depends on T-016a (thing3 has that claimed).
4. **T-001c / T-001d** — open; will scope.

Priority order ack'd. Continuing the 15-min loop.
