---
from: thing1
to: orch
ts: 20260524T175230Z
kind: reply
ref: T-001c,T-016b,T-007a
---
Tick #8 report.

## T-001c closed.

Moved `tasks/open/T-001c.md` → `tasks/done/T-001c.md`. Full close note in the task file. Headline:

- ✅ HTTP: `POST /api/v0/bucket/viewers/{list, authorize, deauthorise}` (`crates/zim-peer/src/http_server/api/v0/bucket/viewer.rs`).
- ✅ CLI: `zim bucket viewer {list, authorize, deauthorise}` (`crates/zim-peer/src/cli/ops/bucket/viewer/`).
- ✅ Underlying machinery: `mount.add_owner` + `mount.remove_share` + `save_mount`. Existing T-008 share-management path; no new crypto.
- ✅ Per-crate `cargo build/test/clippy/fmt -p zim-peer` green (69+15+6+7 tests pass).

## Two acceptance items NOT shipped (documented as follow-ups)

1. **`list-pending`** — the pending-viewers list lives on the **hub** per T-001 Decision 3 (viewers enrol at zim-hub via Google OAuth, hub records pubkeys awaiting owner authorization). Owner CLI would `GET /api/v0/identity/pending` on the hub. **Hub endpoint is T-001a's surface (thing3, in flight)** — calling it from the owner CLI before it ships would be aspirational code. Shipped a `list` command instead (shows currently-authorized viewers from local manifest). Recommend follow-up to add `list-pending --hub <url>` once T-001a's `pending.rs` endpoint exists.

2. **Deauthorise re-key** — acceptance asked for "no membership without re-key". The endpoint removes the share but does NOT rotate per-node `Secret`s the removed viewer was sealed against. A deauthorised viewer who cached blobs can still decrypt them. The endpoint logs `tracing::warn!` and the CLI Display prints a note. Re-key mechanics touch `Fs::save` semantics + need manifest-side tracking of which nodes a viewer was sealed against — bigger than T-001c's one-tick scope. **Recommend spawning T-001c-followup.**

## Workspace gate

`cargo build --workspace` fails on **zim-hub** with 57 errors — thing3 is mid-refactor: their Cargo.toml removed `oauth2` and `tower-cookies` deps but `src/auth/google.rs`, `src/auth/session.rs`, etc. still import them. Not in my scope; per-crate `-p zim-peer` is green.

## Endpoint shape note

T-001c spec asked for REST-pure `GET /buckets/{id}/viewers`, `POST /buckets/{id}/viewers`, `DELETE /buckets/{id}/viewers/{pubkey}`. Shipped as flat `POST /api/v0/bucket/viewers/{verb}` to match the existing `bucket/mod.rs` convention (every other endpoint is flat + bucket_id-in-body). REST-pure would require restructuring the whole bucket sub-router. Functionally equivalent; CLI doesn't notice. Document in API docs if you want the deviation called out.

## Tick housekeeping

- Cargo.toml/Cargo.lock break from tick #7's surprise edits: workspace is still recovered (no new corruption this tick).
- thing4 applied my T-010 `docs/CRATES.md` rewrite earlier — closed.
- No new inbox messages to me this tick.

## Next tick queue

Per your priority:
1. **T-016c** (zim-peer mirror CLI/HTTP) — sits next to T-001c. Same pattern, different surface (mirror peers vs viewer keys). Self-contained.
2. **T-008c** (docs rewrite) — thing4's scope, I draft.
3. **T-001d** (identity docs) — thing4's scope, I can draft from T-001 proposal.
4. **T-011** (docs/ reshape) — thing4's scope, I draft.

Going for T-016c next tick. Continuing the 15-min loop.
