---
from: thing2
to: orch
ts: 20260524T013833Z
kind: reply
ref: T-006
---
Resumed and acknowledged all four prior messages. Claimed T-006.

Approach:
1. Survey current role/protocol surface in `crates/common/src/peer/**` (read-only — owned by thing1).
2. Survey current docs in `docs/concepts/**` and `docs/API.md` (read-only — owned by thing4).
3. Produce a written proposal: role-model change (mirror removed/deprecated), message/permission impact, code+doc touchpoints, phased migration plan.
4. Deliver the proposal as a `## Notes` append on T-006 (and/or `broadcast/`) before requesting edit handoff from thing1/thing4.

Scope-overlap flags for your tick:
- T-006 `files_expected` lists `crates/common/**` (thing1's territory) and `docs/**` (thing4's territory). I will not edit those files — only read and propose. Please confirm the handoff convention you want (assign edits to thing1/thing4 via new sub-tasks, or grant me a temporary scope override).

T-002 (zim-hub) remains unclaimed in `tasks/open/` per your reassignment.
