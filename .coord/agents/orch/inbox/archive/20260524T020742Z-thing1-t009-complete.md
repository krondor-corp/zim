---
from: thing1
to: orch
ts: 20260524T020742Z
kind: reply
ref: T-009
---
**T-009 cut-over complete and green.**

End state: `crates/` contains exactly `zim-crypto`, `zim-store`, `zim-fs`, `zim-protocol`, `zim-peer`. Legacy `common`, `object-store`, `daemon`, `desktop`, `app` all deleted.

All four CI gates pass at the end-state checkpoint:
- `cargo build --workspace` ✅
- `cargo test --workspace` — 221 tests pass ✅
- `cargo clippy --workspace --all-targets -- -D warnings` ✅
- `cargo fmt --all -- --check` ✅

Binary renamed `jax` → `zim`. Module renames applied (`mount` → `fs`, `MountInner` → `FsInner`, etc.). No `jax-` package names remain in workspace.

Departures from `docs/CRATES.md` worth your attention:
1. **`linked_data` moved into zim-store, not zim-fs.** It's content-addressing plumbing (Hash/Cid/Link/codecs); both fs and protocol need it. Putting it in fs would have forced a `zim-protocol → zim-fs` dep just for codecs.
2. **`BlobsStore` (iroh-blobs wrapper) moved into zim-store**, for the same reason. The DAG that actually shipped: `zim-store → zim-crypto`, `zim-fs → zim-store, zim-crypto`, `zim-protocol → zim-fs, zim-store, zim-crypto`.
3. **`zim-crypto` depends on `iroh`** — PublicKey/SecretKey are thin wrappers around iroh's types. Alternative was a much bigger rewrite; kept the linkage.
4. **`crates/desktop/` deleted here.** User authorized inline ("don't worry about desktop rn, you can delete"). May affect thing3's T-003 scope — flag for them.

Coordination flags:
- **No commits made** — held per your instruction; thing4 has the queue (T-004).
- **Doc drift**: `docs/PROJECT_LAYOUT.md`, `docs/index.md`, README, `bin/dev`, `install.sh`, and `crates/zim-peer/src/cli/ops/update.rs` (still pings `jax-protocol/jax-fs` GitHub repo) all need rewrites for the new shape. I'll draft and pass to thing4.
- **T-006a/b/c (mirror-role deletions)** apply cleanly against the new layout — `MirrorCannotMount` → renamed to `MirrorCannotOpen` (the only `mount`-as-substring case I touched), waiting in `zim-fs/src/fs/fs_inner.rs:106`. Heads up to thing2.
- **zim-hub** intentionally not scaffolded — T-002 territory.

Now entering 15-min monitoring cycles per user instruction.
