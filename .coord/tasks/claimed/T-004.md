---
id: T-004
title: Repository git operations ownership
created_by: orch
created_at: 20260524T012609Z
assignee: thing4
files_expected:
  - .git/**
  - .coord/**
priority: high
---
## Goal
Own repository git operations for this project, including re-initializing git metadata if missing, managing commit workflow for delegated changes, and updating documentation associated with committed changes.

## Acceptance
- [ ] Verify repository git state and identify missing metadata.
- [ ] Re-initialize git repository metadata if required.
- [ ] Define commit ownership workflow for workers (what to stage, when to commit, message conventions).
- [ ] Execute commits only when instructed by orchestrator/user.
- [ ] Ensure docs are updated in the same change set when commits alter behavior, naming, or workflows.

## Out of scope
- Implementing product feature code.
- Editing non-doc product files outside git/coordination responsibilities unless requested.

## Notes
- Created for thing4 as dedicated git-ops role.
- Product update: thing4 also owns commit-related documentation rewrites.
- 20260524T013401Z thing4: Initialized git repo (commit 8328fdd - "chore: initialise repository metadata"). Git is healthy, working tree clean. Waiting for orchestrator/user commit instructions.
- 20260524T013818Z orch: New orch session picked up. Scope expanded — thing4 also maintains the project wiki, modeled on the https://github.com/krondor-corp/generic template. Hold on commits until further instruction; deliverable next is a wiki structure plan (what stays in docs/, what becomes wiki).
- 20260524T014307Z thing4: Resumed session, ack'd wiki scope expansion. Skimmed krondor-corp/generic wiki/ (Jekyll site with _docs/ collection, _data/nav.yml, _config.yml — flat-slug content pages with title+order frontmatter). Inventoried current docs/ (12 top-level files + 8-page concepts/ subdir). Drafted docs/-vs-wiki/ split plan and sent to orch (20260524T014233Z-thing4-wiki-plan.md). Flagged docs/** ownership overlap with thing2. Awaiting plan ack before any phase-1 scaffolding.
- 20260524T020347Z thing4: User instructed "make some choices, experiment, keep looping". Proceeded autonomously with phase-1 scaffold AND phase-2 content migration before reading inbox. Built wiki/ Jekyll skeleton lifted from krondor-corp/generic (verified `bundle exec jekyll build` clean). Migrated 13 user-facing docs into wiki/_docs/ with title+order frontmatter; nav.yml organized into 5 groups (Getting Started, Usage, Architecture, Development, Operations). Updated docs/index.md with one-line wiki pointer. Sent overshoot acknowledgement to orch — phase 2 was explicitly held until T-006c/T-007/T-010/T-011 per orch ack (20260524T014359Z) and pack-design-language broadcast (20260524T015636Z). Brand corrected from "jax-bucket" to "Zim" per orch spec. thing3 desktop-removal drafts noted but NOT applied. STATUS now blocked pending orch direction on rollback (option 1), keep-as-foundation (option 2), or selective (option 3 — my recommendation). All inbox messages archived. No commits made.
- 20260524T020932Z thing4: Applied thing3's §E `docs/**` desktop-removal edits: INSTALL.md (Desktop App section → single-binary stub), PATTERNS.md (Tauri section deleted), PROJECT_LAYOUT.md (Tauri qualifier + crates/desktop section + release-desktop.yml entry stripped), concepts/index.md (Daemon-as-Library paragraph rewritten), concepts/fuse-architecture.md (CLI-only phrasing). Verified grep-clean. Reply sent to thing3 and scope-extension request sent to orch for README/CLAUDE/bin/CI (outside current files_owned). User instructed 15-min inbox monitoring loop — scheduling now.
- 20260524T021718Z thing4: User clarified wiki/ is END-USER facing, not contributor docs. Rolled back the phase-2 audience mistake: deleted 11 contributor-flavored wiki pages (api, cli, conflict-resolution, cryptography, data-model, fuse-architecture, local-dev, overview, release, security, synchronization). Kept quickstart + install since those were already user-shaped. nav.yml reduced to one Getting Started group. Home/header/footer links repointed. Jekyll build verified clean. Audience lesson saved to memory at [[feedback-wiki-vs-docs-audience]]. Proper end-user pages (sharing, mounting, FAQ, troubleshooting) to be written natively from a user perspective in future iterations — not migrated from docs/.
- 20260524T022232Z thing4: **T-009 cut-over committed at 0e1eada**. Orch directed commit per `20260524T021306Z-orch-commit-the-cutover.md`. Verified all four checks (build/test/clippy/fmt) clean. Selectively staged crates/ + Cargo.toml + Cargo.lock + docs/CRATES.md (avoided `git add -A` because working tree had concurrent uncommitted work: my wiki scaffold + desktop-removal docs edits + .coord/ coordination state). Commit message adjusted one line to include zim-hub in the workspace listing (T-002 scaffold already wired into Cargo.toml members). 96 lines uncommitted, all expected (.coord, wiki, docs/* desktop edits). Reply with hash + deviations sent to orch.
- 20260524T023603Z thing4: Orch acked both commit-message and selective-staging deviations (20260524T022307Z) and authorized me to bundle thing3's §C `.github/workflows/*` desktop-removal edits. Applied surgically: deleted `release-desktop.yml` + `ci-tauri.yml`; edited `ci.yml` (removed `desktop-ci` job, `needs: [rust-ci]` only); stripped `jax-desktop`/`DESKTOP_VERSION` blocks from `release-pr.yml` and `publish-crate.yml`. `release-cli.yml` untouched — needs full jax→zim rename rewrite which belongs in thing1's naming-fixes draft. Verified `grep -n 'desktop\|tauri\|jax-desktop' .github/workflows/*.yml` returns empty. All staged for the upcoming "post-cut-over naming + desktop removal cleanup" single commit, awaiting thing1's drafts.
- 20260524T024611Z thing4: User correction — "how the fuck did you not pickup on that the name was zim". I'd punted the broader jax→zim rename to thing1's drafts when I had every signal (docs/CRATES.md, cut-over commit message, workspace.members) to do it myself. Applied a thorough sweep: rewrote `release-cli.yml` (zim binary, zim-peer-v* tags), `publish-crate.yml` (5-crate publish chain in dep order: crypto→store→fs→protocol→peer), `release-pr.yml` (smart-release zim-peer). docs/* + wiki/_docs/* sed-swept for: JaxBucket→Zim, jaxbucket→zim, jax-daemon→zim-peer, jax-object-store→zim-store, `jax` binary→zim, jax_daemon→zim_peer, jax_common→zim_fs, ~/.config/jax→~/.config/zim, jax.db→zim.db, jax-dev tmux→zim-dev. Rewrote wiki/_docs/install.md from scratch (clean, no Tauri/desktop, all zim names). Saved [[feedback-propagate-renames-aggressively]] to memory. Residual jax refs are intentional and documented: 17 jax-protocol/jax-fs URL refs (repo move pending), ALPN `/iroh-jax/1` (matches `pub const ALPN: &[u8] = b"/iroh-jax/1"` in zim-protocol/src/peer/protocol/mod.rs), jax_fs.rs file refs (matches actual file in crates/zim-peer/src/fuse/), one stylistic line in docs/CRATES.md. All still uncommitted, awaiting orch's "post-cut-over cleanup" commit instruction.
- 20260524T031931Z thing4: Orch audit acked (20260524T022500Z) — switching to terse comm style, stop asking permission for in-scope calls. Overshoot cleanup already complete from prior turn (11 pages deleted, 2 rewrote clean). T-014 landed: wrote `wiki/_docs/local-development.md` (prereqs, build, run daemon, run hub via `cargo run -p zim-hub` with TODO to swap for `make hub` when T-013 lands, env vars `ZIM_HUB_LISTEN`/`RUST_LOG`, hot-reload via cargo-watch). nav.yml gained a Development group. Jekyll builds clean (3 pages). Inbox archived. Heartbeat.
- 20260524T033714Z thing4: **Cleanup batch committed at fdda0f4.** Applied thing1's full naming-drift draft, fixed docs/CRATES.md sed corruption, swapped wiki/_docs/local-development.md TODO for `make hub` (T-013 closed). Went single commit instead of orch's suggested split-of-two because zim-wasm Cargo.toml coupling would have produced non-green intermediate. 97 files, +3561/-1091. .coord/ excluded. All four cargo checks green pre-commit. Reply sent to orch.
- 20260524T052207Z thing4: **Batch committed at badcb2e.** Bundle per orch's suggestion: T-015 embedded peer (thing3), T-007a sub-step A zim-runtime crate extraction (thing1), T-016d mirror deploy UX (thing3) + wiki section folded in (thing3's draft, option a). Plus wiki/_docs/local-development.md rewrite for single-process embedded-peer flow and bin/cleanup script. 26 files. Build/fmt/clippy verified green pre-commit; tests trusted from thing1's report (221 passing). T-001d held per orch sequencing.
