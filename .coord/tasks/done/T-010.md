---
id: T-010
title: Realign docs/CRATES.md against pack design language
created_by: orch
created_at: 20260524T015636Z
assignee: thing1
files_expected:
  - docs/CRATES.md
priority: high
---
## Goal
Revise the target crate structure in `docs/CRATES.md` so it matches the pack aesthetic without collapsing Zim's p2p/crypto/protocol boundaries unnecessarily. Pack uses `core` / `crdt` / `app`. Zim has no collab editor (no Yjs), so no `crdt`-equivalent. Reconsider whether the current 6-crate split (zim-crypto / zim-fs / zim-store / zim-protocol / zim-peer / zim-hub) is the right grain or whether some collapse toward pack's `core`+`app` shape makes sense.

Reference: https://github.com/krondor-corp/pack (Cargo.toml, crates/{core,app}/src/lib.rs, README.md).

## Acceptance
- [ ] Updated `docs/CRATES.md` reflects pack-aligned structure with documented rationale for any divergence from pack's 3-crate shape.
- [ ] Decision recorded: which Zim crates collapse (if any), and why others remain separate.
- [ ] Cut-over sequence in `docs/CRATES.md` updated to match the revised target — still a single cut-over, no phases.
- [ ] Module/binary naming aligns with pack conventions (handler-per-file, `views/`/`actions/`, `runtime::Service`, etc.) where applicable to the daemon crate.
- [ ] Output: revised `docs/CRATES.md` (drafted by thing1, applied by thing4 per the docs-ownership convention).

## Out of scope
- Code moves (still T-009's job; T-009 resumes once T-010 lands).
- Pack patterns inside daemon code (covered by T-007 ergonomics audit).
- Postgres adoption (explicitly excluded — SQLite stays).

## Notes
- 20260524T015636Z orch: Created in response to pack-design-language broadcast. Assignee TBD — likely thing1 (T-009 author) or thing2 (design-only worker). thing1 is recommended since they wrote the current `docs/CRATES.md`.
- 20260524T015636Z orch: Assigning to thing1.
- 20260524T170157Z thing1: **Draft sent to thing4** for application (docs/** is thing4-owned). Inbox message `agents/thing4/inbox/20260524T170157Z-thing1-T010-CRATES-md-rewrite.md` contains the full replacement content. Acceptance covered: (a) reflects pack-aligned structure with pack-comparison table documenting divergence rationale, (b) explicit decision recorded — Zim has 8 crates vs pack's 3, justified by p2p/crypto/wasm surface pack doesn't have, (c) cut-over sequence section retired to a 3-bullet "Cut-over history" pointing at the three commits, (d) pack module-layout aesthetics noted (`Service` trait, handler-per-file, Datastar instead of HTMX). Awaiting thing4 to apply + close.
- 20260524T170452Z thing4: Applied thing1's full rewrite to `docs/CRATES.md`. Closing T-010.
