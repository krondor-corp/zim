---
id: T-009
title: Execute crate cut-over per docs/CRATES.md
created_by: orch
created_at: 20260524T015113Z
assignee: thing1
files_expected:
  - Cargo.toml
  - crates/zim-crypto/**
  - crates/zim-fs/**
  - crates/zim-store/**
  - crates/zim-protocol/**
  - crates/zim-peer/**
  - crates/common/**
  - crates/object-store/**
  - crates/daemon/**
priority: high
parent: T-005
---
## Goal
Execute the 8-step cut-over defined in `docs/CRATES.md`. End state: workspace contains only `zim-crypto`, `zim-fs`, `zim-store`, `zim-protocol`, `zim-peer`, `zim-hub` under `crates/`. No `common`, `object-store`, `daemon`, `app`, or `desktop` remain. Clean break — no re-exports, no shims, no migration commits.

## Acceptance
- [ ] All source moves and renames executed per `docs/CRATES.md` "Current → target mapping".
- [ ] `crates/common/`, `crates/object-store/`, `crates/daemon/`, `crates/app/` deleted.
- [ ] `crates/desktop/` deletion coordinated with thing3 (T-003); execute the actual delete here.
- [ ] `crates/zim-hub/` left as an empty stub or omitted (covered by T-002 scaffold).
- [ ] Workspace `Cargo.toml` lists exactly the target crates.
- [ ] `cargo build && cargo test && cargo clippy -- -D warnings && cargo fmt -- --check` all pass at end state — single green checkpoint, no per-phase greens.
- [ ] Module renames inside moved code applied (`mount` → `fs`, `MountInner` → `FsInner`, no "core").

## Out of scope
- `zim-hub` implementation (T-002 / T-003).
- T-006 mirror-role deletions (T-006a/b/c) — coordinate ordering but don't fold in.
- Doc rewrites beyond `docs/CRATES.md` (thing4 covers `docs/**` updates after the move).

## Notes
- 20260524T022307Z orch: CLOSED. Cut-over committed at `0e1eada` (thing4). All four cargo checks green. Workspace at zim-crypto/zim-store/zim-fs/zim-protocol/zim-peer plus zim-hub scaffold (T-002). Doc/script naming drift bundled into thing4's next commit.
- 20260524T015113Z orch: Spawned to execute T-005's plan. Assigned to thing1; ROLE.md expanded.
- Coordination: thing3 owns `crates/desktop/**` delete-list (T-003). thing1 executes the deletion when T-003's plan lands. Send a message to thing3 if blocking.
- Coordination: T-006a/b/c (mirror role deletions) target the same code thing1 is moving. Recommend ordering: T-009 lands first (renames + moves), then T-006a/b/c apply to the new locations. Coordinate with thing2.
- Doc updates after the move (e.g. `PROJECT_LAYOUT.md` references) get drafted by thing1 and applied by thing4.
- 20260524T020742Z thing1: **Cut-over complete and green.** All 8 steps of the cut-over executed:
    1. ✅ Source moves per docs/CRATES.md mapping table.
    2. ✅ `crates/common/`, `crates/object-store/`, `crates/daemon/`, `crates/app/`, `crates/desktop/` all deleted (desktop done here without T-003 dependency since user said "don't worry about desktop rn, you can delete").
    3. ✅ Workspace `Cargo.toml` lists exactly: zim-crypto, zim-store, zim-fs, zim-protocol, zim-peer. (zim-hub deferred to T-002 per orch instruction.)
    4. ✅ `cargo build` — clean.
    5. ✅ `cargo test --workspace` — 221 tests pass across 13 binaries (incl. 68+68 mount/integration tests).
    6. ✅ `cargo clippy --workspace --all-targets -- -D warnings` — clean (fixed one `iter_kv_map` warning in manifest.rs).
    7. ✅ `cargo fmt --all -- --check` — clean.
    8. ✅ Module renames: `mount` → `fs`, `MountInner` → `FsInner`, `MountError` → `FsError`, `mount_inner` → `fs_inner`. Binary renamed `jax` → `zim`. Cargo packages renamed `jax-common`/`jax-daemon`/`jax-object-store` → `zim-crypto`/`zim-store`/`zim-fs`/`zim-protocol`/`zim-peer`.
  Departures from docs/CRATES.md plan (worth flagging):
    - `linked_data` ended up in **zim-store** (not zim-fs as the doc stated) — it's pure content-addressing primitives (Hash/Cid/Link/codecs), used by both fs and protocol; moving to zim-store kept the DAG clean and matched its real responsibility. Adjusted DAG: `zim-fs → zim-store, zim-crypto`. `zim-protocol → zim-fs, zim-store, zim-crypto`.
    - `BlobsStore` (iroh-blobs wrapper, was in `common/peer/`) also moved into **zim-store** for the same reason — both zim-fs and zim-protocol need it.
    - `zim-crypto` depends on `iroh` (PublicKey/SecretKey are wrappers around iroh's keys). Kept that linkage; the alternative was a much bigger rewrite.
    - `common/src/version.rs` (BuildInfo) inlined into `zim-peer/src/version.rs` since only the binary needs it.
  Not done (out of scope or coordination-blocked):
    - No commits made (thing4 owns commits per T-004; awaiting orchestrator/user signal).
    - `docs/PROJECT_LAYOUT.md`, `docs/index.md`, root README, `bin/dev` scripts, install.sh, and `crates/zim-peer/src/cli/ops/update.rs` (still references `jax-protocol/jax-fs` GitHub repo) all contain stale jax/daemon naming — these are doc/script updates needing thing4/thing2 follow-up. Draft will be passed to thing4.
    - `crates/zim-hub/` not created (T-002 / per orch).
