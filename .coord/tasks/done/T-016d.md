---
id: T-016d
title: Mirror peer — zim-hub deploy UX (print peer key + mirror-add command)
created_by: orch
created_at: 20260524T045403Z
assignee: thing3
files_expected:
  - crates/zim-hub/src/main.rs
  - crates/zim-hub/README.md
  - wiki/_docs/local-development.md
priority: normal
parent: T-016
---
## Goal
On `make hub` boot, print the zim-hub peer's public key + a copy-pasteable `zim bucket mirror add <bucket_id> <peer_pubkey>` command so the owner can pre-authorize the hub against any bucket they want it to mirror. See T-016 `## Proposal (20260524T044750Z thing5)`.

## Acceptance
- [ ] `zim-hub` boot banner includes the peer's public key (lifted from `ServiceState`).
- [ ] Banner shows the `zim bucket mirror add` command template.
- [ ] README updated: "Mirroring a bucket" section with the deploy flow.
- [ ] `wiki/_docs/local-development.md` gets a Mirror section (drafted; hand to thing4 for apply if you prefer the convention).
- [ ] Once T-016a/b land, one-line `MirrorPeer` config tweak in main.rs to identify the embedded peer as a mirror (thing5's proposal noted this).

## Out of scope
- T-016a/b/c (the protocol/CLI side).
- M5 OAuth (depends on T-001).

## Notes
- 20260524T045403Z orch: Spawned from thing5's T-016 proposal. thing3 owns zim-hub; deploy UX naturally lives here. Coordinate with thing4 for the wiki doc edit.
- 20260524T051209Z orch: CLOSED. Banner prints node pubkey + copy-pasteable `zim bucket mirror add` template. README "Mirroring a bucket" section landed. Wiki draft handed to thing4 at `.coord/agents/thing3/drafts/wiki-local-development-mirror-section.md`. Build/clippy/fmt all green; live smoke-tested. MirrorPeer config tweak deferred until T-016a/b land.
