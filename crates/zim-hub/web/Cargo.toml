[package]
name = "zim-hub-web"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false
description = "Yew (Rust/WASM) frontend for zim-hub. Consumes zim-hub-wasm as the SDK."

# Standalone (own workspace): a wasm32-only Yew app built by Trunk, kept out
# of the host `cargo build --workspace`. The empty [workspace] table below
# makes it its own root so it doesn't inherit the parent workspace.
[workspace]

[dependencies]
yew = { version = "0.21", features = ["csr"] }
yew-router = "0.18"
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
js-sys = "0.3"
web-sys = { version = "0.3", features = [
    "Window",
    "Location",
    "HtmlInputElement",
    "File",
    "FileList",
    "Blob",
    "BlobPropertyBag",
    "Url",
    "Navigator",
    "Clipboard",
] }
gloo-storage = "0.3"
gloo-net = "0.6"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
console_error_panic_hook = "0.1"

# The SDK: HubClient / WasmFs / crypto, called directly as Rust.
zim-hub-wasm = { path = "../wasm" }
# Shared hub wire types (types-only — no reqwest, so it builds on wasm32).
# We deserialize the same `Device` the hub server serializes + the CLI reads.
zim-api = { path = "../../zim-api", default-features = false, features = ["hub"] }
