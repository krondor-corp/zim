---
id: T-014
title: wiki — local development page
created_by: orch
created_at: 20260524T024155Z
assignee: thing4
files_expected:
  - wiki/_docs/local-development.md
  - wiki/_data/nav.yml
priority: normal
parent: T-004
---
## Goal
Write `wiki/_docs/local-development.md`: how a user runs Zim locally (zim-peer daemon + zim-hub web server). Practical, hands-on, copy-pasteable. NOT architecture commentary.

## Acceptance
- [ ] `wiki/_docs/local-development.md` exists, single page, focused on "do this to get it running."
- [ ] Covers (as much as currently exists):
  - Prerequisites: Rust toolchain, any system deps.
  - Build: `cargo build --workspace`.
  - Run the daemon: command + default ports.
  - Run zim-hub: `make hub` (the new T-013 target) + default URL.
  - Common edits / hot reload behavior.
  - Useful env vars (`ZIM_HUB_LISTEN`, `RUST_LOG`).
- [ ] Frontmatter: `title: Local Development`, `order:` integer matching the Development nav group.
- [ ] `wiki/_data/nav.yml` updated: add a "Development" group with `local-development` slug.
- [ ] No architecture content. No "how Zim works" essays. No protocol explanations. No CRDT theory. If the user wants that, they go to `docs/concepts/` (contributor-facing).

## Out of scope
- **Big sweeping architecture docs.** Explicitly excluded per user direction.
- The `concepts/`, `architecture/`, `reference/` subdir reshape — that's T-011.
- Any other wiki page beyond `local-development.md`.

## Notes
- 20260524T032350Z orch: CLOSED. `wiki/_docs/local-development.md` landed with prereqs, build, run-daemon, run-hub (with TODO to swap `cargo run -p zim-hub` for `make hub` — T-013 since landed; swap should follow), env vars, hot-reload notes. nav.yml gained Development group. Jekyll build clean (3 pages). Follow-up: update the page to use `make hub` since T-013 is in.
- 20260524T024155Z orch: Created in response to user direction. Wiki is end-user facing; this page is operational ("how do I run it"), not theoretical.
- Coordinate with T-013 (thing3): if T-013 lands before you draft, document `make hub` as the canonical command. If you draft first, leave a `<!-- TODO: replace with `make hub` once T-013 lands -->` and write `cargo run -p zim-hub` as the interim command.
- Wiki overshoot cleanup (per `20260524T022500Z-orch-audit-fix-the-overshoot.md`) takes priority — finish that first. Then this.
- Keep it short. One page. Reads in 5 minutes. Don't over-engineer.
