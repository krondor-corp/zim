---
id: T-016a
title: Mirror peer — zim-fs (mirrors field + classify_peer + PeerType)
created_by: orch
created_at: 20260524T045403Z
assignee: thing3
files_expected:
  - crates/zim-fs/src/fs/manifest.rs
  - crates/zim-fs/src/fs/principal.rs
priority: high
parent: T-016
---
## Goal
Land the data-model half of T-016's mirror-peer-type design in `zim-fs`. See T-016 `## Proposal (20260524T044750Z thing5)` for full design.

## Acceptance
- [ ] `Manifest` gains `mirrors: Vec<PublicKey>` field.
- [ ] New `PeerType { Owner, Mirror, Anonymous }` enum (in `principal.rs` or new module).
- [ ] `Manifest::classify_peer(pk: &PublicKey) -> PeerType` method — looks up `pk` in `shares` (Owner if present) else in `mirrors` (Mirror if present) else `Anonymous`.
- [ ] `Share::new_mirror` and related Mirror-share construction deleted (these were left from T-006a's pending close — T-016a unblocks it).
- [ ] `cargo build/test/clippy/fmt` all green workspace-wide.

## Out of scope
- Wire-protocol gating (T-016b).
- Daemon CLI/HTTP for mirror add/remove/list (T-016c).
- zim-hub deploy doc (T-016d).
- Implementation pattern: NO `PrincipalRole::Mirror` revival.

## Coordination
- **T-016a lands first**, then **T-006a** can cleanly delete the `PrincipalRole::Mirror` enum (T-006a still has dangling references to it in zim-fs internals). thing5's proposal flagged this: T-016a unblocks T-006a, not the other way around.
- **T-016b** (thing1, next) depends on this.

## Notes
- 20260524T045403Z orch: Spawned from thing5's T-016 proposal. Assigned to thing1 (owns zim-fs).
